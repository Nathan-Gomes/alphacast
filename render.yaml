services:
  - type: web
    name: alphacast
    runtime: python
    buildCommand: pip install .
    startCommand: uvicorn alphacast.app:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /api/health
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.8
